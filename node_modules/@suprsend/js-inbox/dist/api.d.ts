import { Socket } from 'socket.io-client';
import SuprSendInbox from '.';
import { IStore } from './types';
export default class ApiClient {
    socket: Socket;
    config: SuprSendInbox;
    constructor(config: SuprSendInbox);
    private orderNotifications;
    private _initializeSocketEvents;
    private _notificationBelongToStore;
    private _getStoreQueryString;
    private _getStoresQueryString;
    getNotificationsCount(): Promise<Response>;
    getNotifications(store: IStore | undefined, page_no: number, page_size: number | undefined, before: number): Promise<Response>;
    getNotificationDetails(notification_id: string): Promise<Response>;
    markBellClicked(): Promise<Response>;
    markAllRead(): Promise<Response>;
    markNotificationClicked(id: string): Promise<Response>;
    markNotificationRead(id: string): Promise<Response>;
    markNotificationUnRead(id: string): Promise<Response>;
    markNotificationArchive(id: string): Promise<Response>;
}
